classdef Drain
    %DRAIN Summary of this class goes here
    %   Detailed explanation goes here
    
    properties
        mean
        stdev
        N
    end
    
    methods
        function obj = Drain(bit_sequence)
            obj.N = length(bit_sequence);
            mn = mean(bit_sequence, N);
            stdev = std(bit_sequence);
        end

        function a = get.mean(self)
            a 
        end
    end
end

